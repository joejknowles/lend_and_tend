= form_for current_user, url: users_edit_profile_path,  html: { method: :put } do |f|
  = f.label :about_me
  = f.text_area :about_me, size: '50x6', placeholder: 'Do you smoke?'
  = f.submit 'Save profile'