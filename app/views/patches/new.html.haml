= form_for @patch do |f|
  = f.label :location
  = f.text_field :location, placeholder: "Postcode, e.g. 'WF12 7ED'"
  = f.label :size
  = f.text_field :size, placeholder: "Size, e.g. small"
  = f.label :duration
  = f.text_field :duration, placeholder: "Duration in years"
  = f.submit 'Add'