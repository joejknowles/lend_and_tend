%h2 Add images for the homepage here
%p  Please be patient whilst they upload, there must be at least X images in this list else the homepage will break.

= form_for @homepage_image, html: { multipart: true}  do |f|
  = f.label :image
  = f.file_field :image
  = f.submit 'Add photo'